[tool.pytest.ini_options]
# `runs/**/working/` 에 남는 실험 사본에 같은 이름의 self_test.py 가 여럿 있어
# 저장소 루트에서 pytest 를 그냥 치면 수집 단계에서 멈춘다. 검사 대상을 못 박는다.
testpaths = ["tests"]
norecursedirs = ["runs", "data", "docs", ".git", ".venv", "__pycache__"]
