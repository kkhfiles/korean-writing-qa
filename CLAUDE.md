# korean-writing-qa 프로젝트 규칙

**이 파일의 범위** — 이 저장소에서 일할 때만 적용 · 공통 원칙은 전역 `CLAUDE.md`

---

## ★ 이 프로젝트가 지키는 것 (2026-09-16 사용자 확정)

> **어떤 방법을 쓰든, 약간의 토큰 비효율이 있더라도, 이 PC에서 만드는 문서나
> 이 오픈소스를 설치한 PC에서 만드는 문서에 문제가 되는 한글이 나오면 안 된다.**
>
> (사용자 2026-09-16)

**우선순위** — 이 문장이 다른 모든 판단보다 앞선다.

### 여기서 따라 나오는 것

- **비용은 근거로 안 씀** — 「문서당 지적이 스물몇 개라 부담이 크다」는 이유로 검사를 약하게 하지 않는다. 부담이 크면 **줄이는 길을 찾되 잡는 힘을 낮추지 않는다.**
- **토큰·시간 비효율은 감수** — 검사를 한 번 더 돌리는 것과 잘못된 문서가 나가는 것 중에서는 앞엣것을 고른다.
- **침묵은 합격이 아님** — 검사기가 조용한 것은 그 규칙을 안 봤다는 뜻일 수 있다.
- **이 저장소 밖에서도 돌아야 함** — 설치한 PC에서 같은 보호가 걸려야 하므로, 검사·목록·기록은 `install.py` 가 옮기는 대상에 들어간다.

### 판단이 갈릴 때

| 상황 | 고르는 쪽 |
|---|---|
| 오탐이 늘고 미탐이 주는 선택 | **미탐 축소** · 오탐은 사람 몫 |
| 막으면 일이 느려지는 선택 | **막음** · 푸는 길을 함께 두되 그 기록이 남게 함 |
| 자동 판정이 어려워 사람에게 넘길 때 | **넘길 곳을 명시** · 안 적으면 그 순간부터 미탐 |
| 규칙을 글로 적을까 기계로 옮길까 | 같은 함정에 두 번 걸렸으면 **기계** |
| 빈도 실측과 글의 품질이 엇갈릴 때 | **품질** · 빈도는 참고 |

### ★ 빈도는 기준이 아니라 참고 (2026-09-18 사용자 결정)

**규칙의 근거는 「잘 쓴 문서인가」** — 「사람이 많이 쓰니 괜찮다」는 근거로 쓰지 않는다. 빈도 실측은 그 판단을 돕는 자료다.

- **왜** — 빈도를 기준으로 삼으면 널리 퍼진 나쁜 습관이 그대로 통과한다. 많이 쓴다는 것은 좋다는 증거가 아니라 흔하다는 사실이다.
- **논증 순서** — 먼저 「이 표현이 좋은 글에 맞나」를 적고, 빈도는 뒤에 보강으로 붙임. 순서가 바뀌면 논증이 빈도에 끌려간다.
- **빈도가 높다는 이유로 빼려면 다른 근거 필요** — 모호하지 않다거나, 굳은 표기라 바꾸면 정본과 어긋난다거나.
- **빈도가 낮으면 강한 보강** — 다만 「드물다」가 아니라 **왜 안 쓰는 말인지**를 적는다.
- **기준선에 대화를 섞지 않음** — 메일·이슈 댓글은 대화라 지시어가 자연스럽게 많다. 문서 검사기의 기준선은 **문서**여야 한다.

### ⛔ 하지 말 것

- **열쇠 없는 자물쇠** — 통과할 길을 안 만들고 막지 않는다. 2026-09-16 에 발행 게이트가 세 단계를 요구하면서 두 단계를 적는 길을 안 만들어, 모든 발행이 막히고 통로가 넘기기 하나뿐인 상태로 배포했다. **넘기기가 습관이 되면 그 뒤로는 어떤 게이트도 안 듣는다.**
- **단어를 하나씩 규칙에 더하기** — 여섯 차례 그렇게 실패했다. 일곱째 단어를 못 막는다.
- **정상 판정을 단어 단위로 열기** — 「갈래는 정상」으로 등록하면 「갈래 ① 변경 요청 단계」까지 통과한다. **문맥과 적용 범위를 함께** 적는다.
- **증상이 안 나는 조건에서 재고 「안 난다」고 적기** — 게이트는 `Temp`·`scratchpad` 를 건너뛰고 「파일당 세션 1회」로 둘째 호출을 접는다. 시험은 `--probe` 로 부른다.

---

## 검사 층과 각자 맡은 것

| 층 | 실행체 | 잡는 것 | 회수율 |
|---|---|---|---|
| 1층 · 구조 | `assets/doc-style-check.py` | 개조식·절단형·지어낸 명사구 | 0.057 |
| 1층 · 단어 | 같은 실행체의 고정 짝 목록 | 이미 짚힌 짝 | — |
| 단어 점검 | `scripts/rare_words.py` | 업무 글에서 드문 단어 · **새 단어를 잡는 유일한 기계** | — |
| 2층 · 판단 | 모델이나 사람이 `core-rules.md` 를 읽음 | 문맥을 봐야 갈리는 것 | 0.771 |

- **분업 기준** — 글자 대조로 갈리면 1층 · 문맥을 읽어야 갈리면 2층
- **통과 기록** — `scripts/check_record.py` 가 내용 해시에 묶어 적는다. 한 글자만 고쳐도 안 맞는다.
- **★ 주의를 읽고 내린 판단도 기록** (2026-09-18) — `--stage review --verdict pass --note "무슨 주의를 왜 정상으로 봤나"`. 발행 직전 검사가 **주의만 있을 때** 이 기록을 보고 통과시킨다.
  - **왜 필요한가** — 주의는 「사람이 보고 판단하라」는 등급인데 그 판단을 받아 줄 곳이 없었다. 오탐이라고 봐도 문구를 지우거나 게이트를 넘기는 길뿐이었고, 다시 돌려도 같은 주의가 나와 영원히 막혔다.
  - **⛔ 오류에는 안 들음** — 오류는 사람 판단 대상이 아니다.
  - **⛔ 까닭 없으면 거절** — 고무도장이 되면 넘기기와 같아진다.

---

## 일할 때 지키는 절차

- **전역 자산을 고쳤으면 `python install.py`** — 설치본이 어긋나면 다른 프로젝트에서 옛 판이 돈다.
- **시험은 `python -X utf8 -m unittest discover -s tests`** — 파이프로 넘기지 않는다(종료 코드가 가려진다).
- **공개 저장소이므로 발행 전 `python -X utf8 scripts/check_publish_safety.py`** — 사람 이름·고객사는 저장소 밖 목록으로 본다.
- **표현 하나를 짚어 주면 그 하나만 고치지 않음** — 같은 갈래를 전수로 찾고 대장(`data/cases/flagged-rounds.jsonl`)에 올린다.
