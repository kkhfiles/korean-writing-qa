# 제품·상표 이름의 정본 표기 — 본보기
#
# 이 파일을 `local-brand-names.txt` 로 복사해서 쓰십시오.
#
#     cp data/catalog/local-brand-names.example.txt data/catalog/local-brand-names.txt
#
# 복사본은 `.gitignore` 에 있어 저장소에 안 올라갑니다. **이름을 코드나 공개
# 저장소에 적지 않기 위해서**입니다 — 규칙은 모두가 나눠 쓰고 이름은 각자
# 자기 기계에만 둡니다. 없으면 이 갈래는 안 돕니다(검사기가 그 사실을 알립니다).
#
# ── 적는 법 ─────────────────────────────────────────────────────────────
#
#     정본 = 잘못 적은 꼴, 또 다른 꼴
#
# 왼쪽이 문서에 나와야 하는 표기, 오른쪽이 잡아야 하는 표기입니다.
# `#` 로 시작하는 줄과 빈 줄은 건너뜁니다.
#
# ── 넣을 것 ─────────────────────────────────────────────────────────────
#
# **자사 제품·서비스 이름만** 넣으십시오. 음차가 굳은 일반 도구 이름은 넣지
# 마십시오 — 「노션」·「슬랙」·「지라」는 업무 한국어에 정착한 정상 표기입니다.
# 실측(문서 272개) — 노션 96회 · 슬랙 72회 · 지라 56회가 전부 정당했고,
# 음차가 실제로 문제였던 것은 자사 제품 이름 하나뿐이었습니다.
#
# ── 보기 (지우고 자기 것을 넣으십시오) ──────────────────────────────────

ACME = 애크미, 액미
FooBase = 푸베이스, 후베이스

# ── 안 넣는 편이 나은 것 ────────────────────────────────────────────────
#
# Notion = 노션      ← 정착한 표기라 넣으면 정상 문서가 걸립니다
# Slack  = 슬랙
# Jira   = 지라
