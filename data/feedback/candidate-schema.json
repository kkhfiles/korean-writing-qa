{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://local.invalid/korean-writing-qa/feedback-candidate.schema.json",
  "title": "Korean writing feedback candidate",
  "type": "object",
  "required": [
    "candidate_id",
    "record_kind",
    "rule_id",
    "original",
    "revised",
    "scope",
    "category",
    "context_level",
    "decision_source",
    "user_verdict",
    "status",
    "replacement_mode",
    "requires_context"
  ],
  "properties": {
    "candidate_id": {"type": "string", "minLength": 1},
    "record_kind": {"const": "language_rule"},
    "rule_id": {"type": "string", "minLength": 1},
    "detector": {"enum": ["literal", "regex"]},
    "pattern": {"type": "string", "minLength": 1},
    "allow": {
      "type": "array",
      "items": {"type": "string", "minLength": 1},
      "description": "이 형태로 나오면 지적하지 않는다. 오탐을 규칙에 되먹이는 통로다."
    },
    "allow_examples": {
      "type": "array",
      "items": {"type": "string", "minLength": 1},
      "description": "허용 형태의 실제 예문. 회귀 시료가 된다. allow 와 같은 순서."
    },
    "original": {"type": "string", "minLength": 1},
    "revised": {"type": "string"},
    "scope": {"type": "string", "minLength": 1},
    "profile": {
      "enum": [
        "general",
        "technical-report",
        "executive-report",
        "presentation",
        "presenter-notes",
        "short-message",
        "ct-project"
      ]
    },
    "category": {"type": "string", "minLength": 1},
    "context_level": {
      "enum": ["sentence", "window", "block", "document"]
    },
    "decision_source": {
      "enum": ["user_direct", "user_accept", "human_review", "model_candidate"]
    },
    "user_verdict": {
      "enum": ["observed", "corrected", "accepted", "false_positive"]
    },
    "status": {"enum": ["observed", "confirmed", "active", "retired"]},
    "replacement_mode": {"enum": ["exact_suggestion", "contextual_rewrite"]},
    "requires_context": {"type": "boolean"},
    "context_before": {"type": "string"},
    "context_after": {"type": "string"},
    "context_excerpt": {"type": "string"},
    "section_title": {"type": "string"},
    "logical_path": {"type": "string"},
    "source_ref": {"type": "string"},
    "source_hash": {"type": "string"},
    "note": {"type": "string"}
  },
  "additionalProperties": false,
  "allOf": [
    {
      "if": {"properties": {"detector": {"const": "regex"}}, "required": ["detector"]},
      "then": {"required": ["pattern"]}
    }
  ]
}
