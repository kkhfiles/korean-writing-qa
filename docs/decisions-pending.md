---
form: structured
---
# 판단이 필요한 것 — 2026-08-31 갱신

**남은 것 둘 · 둘 다 사용자 결정** — 아래 「지금 남은 것」만 읽으면 됨 · 그 밖은 끝난 기록이고 근거를 남기려 둔 것

## 지금 남은 것

| 무엇 | 왜 지금 | 추천 | 판단 |
|---|---|---|---|
| 후보 11건을 1층 규칙으로 승격할지 | 판단 층 표에는 이미 실려 2층이 읽는 중 · 1층으로 올릴지는 별개 | 보류 — 낱말 대 낱말 치환이 안 되는 것이 실측으로 확인됨 | |
| 「포지셔닝 → 제품 설명」 | 판정 유보 상태 · 「어떤 맥락인지 확인 필요」 | 원문 맥락을 보고 정할 것 | |

**혼자 도는 것 하나** — 2층 안내 효과 재측정. 사용자 판단이 아니라 며칠 뒤 `scripts/measure_layer2_uptake.py` 를 다시 돌리면 됨

## 끝난 결정 — 근거 기록

| 번호 | 무엇 | 결과 |
|---|---|---|
| ~~1~~ | ~~훅이 정당한 지적을 조용히 감춤~~ | 접히는 문서 44 → 7 · 지적 1,277건 회복 |
| ~~2~~ | ~~백서·설명문 문서 종류 없음~~ | 문서 종류 표에 가는 길 추가 |
| ~~3~~ | ~~확정 교정 4건이 어디에도 안 실려 있음~~ | 표 4행 추가 + 누락 방지 시험 |
| ~~4~~ | ~~scope 한 축에 여러 뜻이 섞여 있음~~ | **안 함** · 「공통」 처리가 이미 구현돼 있었음 |
| ~~5~~ | ~~회수율 분모가 「누가 알아챈 것」임~~ | **해결** · 세 문서 전수 판정으로 분모가 참이 됨 (`runs/eval-005`) |
| ~~6~~ | ~~산문에서 「우리」를 계속 지적할지~~ | **유지** · 산문에서도 1.9배 · 표본이 정당 |

---

## 1. 훅이 정당한 지적을 조용히 감추고 있음

**처음 적은 것보다 큼** — 「정의 두 벌이 어긋난다」로 적었는데, 실제로는 **발행 시점에 문서 11.6%에서 맞는 지적이 사라진다.**

**접는 조건** — 서술형 지적이 8건 넘고 전체의 40% 이상 (`~/.claude/hooks/doc-style-gate.py`)
**걸린 문서** — 378건 중 44건 · 그중 **40건이 산문 비율 0.30 미만** · 표와 라벨이 뼈대인 문서에 근거 문단이 붙은 형태 · 표본으로 열어 본 그 구간의 지적은 정당했음

| 접힐 때 사라지는 것 | 건수 | 형식축이었다면 |
|---|---|---|
| 개조식 부류 · 구조 문서에서 접힘 | 1,043 | 남음 — 그 문서는 산문이 아님 |
| 형식과 무관한 부류 | 237 | 남음 — 산문에서도 잘못 |
| 그중 「값 안 굵게」 | 182 | 남음 |

**발행 시점에 걸린다는 것이 특히 나쁨** — 쓰는 순간에는 훅이 원래 매체 무관 규칙만 낸다(소음 억제). 접기는 발행 직전에만 도는데, 공유 자료의 오류 0을 확인하는 그 순간에 판이 가려진다.

**수치를 낮춰 읽을 여지** — 훅은 어차피 12줄만 보여 준다. 1,280건이 화면에서 사라지는 것이 아니라 **보여 줄 12줄을 고르는 후보에서 그 부류가 통째로 빠진다.** 그래도 접힌 부류는 한 줄도 못 뜬다.
**근거의 한계** — 0.15~0.30 구간의 지적이 정당하다는 판단은 문서 6건을 열어 본 표본이다. 전수 확인은 안 했다.

**처리 완료 (2026-08-27)** — 훅의 세는 방식을 지우고, 검사기가 「형식 미표기」로 물어볼 때만 산문으로 다시 묻게 했다. 무엇을 뺄지는 검사기가 정하므로 목록이 한 벌이다.

| 측정 | 전 | 후 |
|---|---|---|
| 접히는 문서 | 44건 (11.6%) | 7건 (1.9%) |
| 가려지던 지적 | 1,280건 | 회복 1,277건 |

기록 `runs/diagnostic-008` · 시험 6건 · 돌연변이 둘로 확인

## 2. 백서·설명문 문서 종류 없음 — 처리 완료

**전용 프로필을 안 만듦** — 새 값을 만들어도 규칙 묶음이 `general_it_business` 그대로라 검사 결과가 달라지지 않는다. 이름만 늘리는 일이다.

**대신 가는 길을 적음** — `document-types.md` 에 「기술 백서 → `technical-report` + 머리말 `form: prose`」 · 「일반 설명문 → `general` + `form: prose`」. 안 적어 두면 찾다가 못 찾는다.
**시험** — 그 안내가 지워지면 잡는다.

## 3. 확정 규칙 6개가 쓸 수 없는 층에 놓여 있음

**앞서 「발화 0회라 재배치 효과 없음」이라 적은 것을 철회함.** 발화 횟수로 규칙의 가치를 재려 한 것이 틀렸고, 잰 방법도 틀렸다.

**측정이 틀린 두 곳**

| 어긋난 곳 | 잘못된 방식 |
|---|---|
| 집단 | 발표 대본용 규칙 6개를 **보고서 뭉치**에 대고 쟀음 |
| 결론 | 다시 재도 판정이 안 나는데 「0회」를 근거로 씀 |

**제대로 재도 판정이 안 나는 이유** — 뭉치 안의 발표 자료는 `main.md` 한 건뿐이고, 규칙을 뽑아낸 그 파일과 **해시가 같다**(이미 고친 뒤다). 즉 「대상에 없음」이 아니라 **판정 불가**다. 같은 종류의 발표 대본이 더 있어야 잴 수 있다.

**대신 확인된 진짜 문제 — 기록이 스스로 「문맥이 필요하다」고 말하는데 글자로 찾고 있음**

| 기록에 적힌 것 | 값 |
|---|---|
| `replacement_mode` | `contextual_rewrite` |
| `requires_context` | `true` |
| 실제 탐지 방식 | 원문 그대로 문자열 대조(`literal`) |
| 원문 길이 | 10~47자 · 통째로 한 발화 |

**47자짜리 발화는 다음에 똑같이 안 나옴** — 못 도는 게 아니라 돌 수 없는 형태로 저장돼 있음

**사람이 읽는 표는 이미 줄여 놓았음** — `core-rules.md`의 「사용자가 확정한 대표 수정」 표는 같은 교정을 「갈래는 둘」(5자)·「검증은 반복해서 도는 일」(13자)로 담고 있다. 기계 쪽 사본만 46자·26자 통문장이다. **사람 쪽이 재사용 가능한 핵심을 이미 뽑아 놓았고 기계 쪽이 그걸 못 따라갔다.**

**실측 결과 — 재승격도 안 됨** (`runs/diagnostic-006`)

| 후보 형태 | 발화 | 판정 |
|---|---|---|
| 표가 뽑아 둔 5~13자 그대로 | 0 | 그 발화가 다시 안 나옴 |
| 동사로 일반화(돌다·풀다·진행하다·붙다) | 124 | 잡힌 문장 전부 정상 한국어 · 정밀도 사실상 0 |

**「돌다」 34건 전수 확인** — 「검사기를 돌린다」·「훅이 sync.sh를 돌립니다」처럼 표준 쓰임뿐

**좁히면 다시 안 나오고 넓히면 정상 문장을 잡음 — 쓸 수 있는 중간이 없음**

**집단은 비어 있지 않았음** — 경어체 종결로 센 발화체가 538줄 있다. 앞선 「판정 불가」와 달리 여기서는 잴 수 있었고, 잰 결과가 위와 같다.

**그래서 이 6건의 자리는 탐지기 층이 아니라 판단 층** — 기록이 이미 `requires_context: true`로 그렇게 적어 두었다.

**진짜 빈 곳** — 판단 층이 실제로 읽는 `core-rules.md` 「사용자가 확정한 대표 수정」 표에 **6건 중 2건만** 있다. 나머지 4건(과정 압축·지시 대상 생략·계획을 결과처럼·이유 압축)은 어디에도 안 실려 있어 판단 층이 못 본다.

**처리 완료 (2026-08-27)** — 빠진 4건을 표에 넣었고, 문맥 교정이 표에서 빠지면 잡는 시험을 붙였다. 새 정책이 아니라 이미 확정된 교정을 읽히는 곳으로 옮긴 것이라 판단 대상에서 뺀다.
**폐기한 안** — `--pattern`으로 재승격 · 위 실측으로 근거 없음

## 4. scope 한 축에 여러 뜻이 섞여 있음 — 안 함

**원래 물음** — 「공통도 있을 텐데 코드처럼 잘 분류해 넣을 수 없나」. 이 프로젝트를 시작하게 한 질문이다.

**이미 구현돼 있었음** — `applies_to_scope` 를 읽어 보니 `general_it_business` 가 항상 켜지는 바탕이고 다른 scope 가 그 위에 얹힌다. 「안 적으면 공통」을 값 이름으로 표현했을 뿐 동작은 그것이다.

| 프로필 | 활성 규칙 | 확인 |
|---|---|---|
| `general` 등 | 공통 1건 | 바탕 규칙 하나 |
| `ct-project` | 공통 + CT 1건 | 실제로 2건 지적 |
| `presenter-notes` | 공통 + 발표 대본 6건 | 6건이 발화 0회 |

**남은 겹침 하나** — `presenter_notes` scope 는 형식 축(`form: prose`)과 뜻이 겹친다. 다만 그 scope 의 규칙 6건이 어떤 형태로도 안 돌므로(3번 참조) 없애도 관측되는 변화가 없다.

**안 하는 이유** — 바꿔도 결과가 같은 설정을 손대는 일이다. 규칙이 실제로 늘어 축이 필요해지면 그때 한다.

## 5. 회수율 분모가 「누가 알아챈 것」임 — 처리 완료

**지금 회수율의 뜻** — 라벨 13건이 전부 사람·모델의 읽기 통과에서 나왔다. 그래서 「사람이 찾아낸 것 중 스크립트가 잡는 비율」이지 「문서에 있는 문제 중 잡는 비율」이 아니다.

**참값을 재려면** 문서 몇 건을 줄 단위로 전수 판정해야 한다 — 라벨이 붙은 문서 3건의 본문이 133행이다.
**비용** 사용자 시간 한두 시간
**처리 완료 (2026-08-31)** — 문서 3건을 줄 단위로 전수 판정 · 분모가 「그 줄들에 실제로 있는 문제」가 됐다 · 1층 0.057 · 2층 0.771 (`runs/eval-005`)
**남은 한계** — 그 세 문서 안에서만 참이다. 참 문제 35건이라 신뢰 구간이 넓다.

## 6. 산문에서 「우리」를 계속 지적할지 — 유지

**실측** — 산문 문서에서 문서당 0.15건 · 구조 문서 0.08건 · 1.9배. 표본 2건을 열어 보니 둘 다 실제로 모호했다(「우리 케이스」 등).

**유지하는 이유** — 오탐이면 그 문장 하나를 고치면 되고, 끄면 부서가 섞여 보는 사내 문서에서 진짜 모호한 「우리」를 놓친다. 잃는 쪽이 더 크다.

## 시각 서식 검사 제거 — 처리 완료 (2026-08-28)

**사용자 결정** — 「시각 서식까지 보는 건 이 검사 목적과 일치하지 않음」. 검사기가 보는 것은 낱말과 문장뿐

| 검사 | 처리 | 실측 건수 |
|---|---|---|
| 값 안 굵게 | 뺌 | 1,862 |
| 묶음 과대 | 뺌 | 226 |
| 강조 과다 | 뺌 | 151 |
| 스캔 가치 없는 라벨 | **오류 → 주의** | 180 |

**라벨 규칙을 남기되 낮춘 이유** — 시각이 아니라 낱말 선택이라 값이 있음. 다만 걸린 180건의 57%가 표 머리 「비고」인데 공문서 표의 표준 관례라 발행을 막을 근거가 못 됨

**측정 (문서 380건 · html 9건)**

| 항목 | 전 | 후 |
|---|---|---|
| 오류 | 1,707 | 1,527 |
| 주의 | 2,546 | 487 |
| 오류 없는 문서 | 147 | 207 |
| 발행 화면이 굵게 지적만인 문서 | 9% | 0% |

**남은 종류의 건수는 하나도 안 바뀜** — 서술형 종결 1,273 · 절단형 종결 91 · 모호한 지칭 67 등 전부 동일

**바꾼 곳** — `~/.claude/assets/doc-style-check.py` · 전역 CLAUDE.md §4·§검사기 · `document-types.md`
**검증** — 돌연변이 9건 전부 잡힘(뺀 검사 되살리기 6 · 라벨 등급 되돌리기 3 · 낱말 검사 끄기 1) · 시험 149건
**남은 어긋남 없음** — 자체 검사기 쪽 `severity` 값은 읽는 코드가 없는 죽은 데이터 · 스킬 규칙 문서에는 라벨 항목이 애초에 없음

## 줄 단위 판정 — 처리 완료 (2026-08-31)

**판정 8건 반영** — 회수율은 `runs/eval-005` · 판정 기록은 `data/annotations/eval-004-line-review.jsonl`

| 층 | 회수율 |
|---|---|
| 1층 | 0.057 |
| 2층 | 0.771 |

**미탐 6건 처리** — 판단 층 표에 조건과 함께 6행 추가 · 낱말 대 낱말 치환은 안 만듦(사용자도 「이슈」·「컨펌」·「타깃」을 쓰므로 문맥이 가름)
**오탐 1건 처리** — 「에이전틱」을 첫 KEEP 시료로. 자체 검사가 `FIX` 말고는 안 받게 막고 있어 그것부터 열었음
**보류 1건** — 「포지셔닝 → 제품 설명」 · 「어떤 맥락인지 확인 필요」로 판정 유보

## 검사 범위 — 확정 (2026-08-31)

**대상은 문서로 나갈 때뿐** — 보고·공유·제출용으로 글이 파일에 적히는 순간 · 터미널 응답과 대화는 대상 아님

**닫은 안** — 빈 명사(판·창·값) 검사를 검사기에 넣기 · 터미널 응답에서는 7.9배로 튀지만 문서에서는 사용자 0.90 대 내 0.52로 오히려 적음 · 넣어도 정상 문장만 잡힘
**응답 쪽 규율** — 글로벌 CLAUDE.md 소관 · 이 저장소가 맡지 않음

## 재검토에서 고친 것 — 2026-08-27 오후

| 항목 | 처음 적은 것 | 확인 결과 |
|---|---|---|
| 1 | 「정의 두 벌이 어긋남」 | **축소해 적었음** — 발행 직전 점검에서 문서 11.6%의 지적 1,280건이 후보에서 빠짐 |
| 2 | `whitepaper` 프로필 추가 | **추천 뒤집음** — 오늘 넣어도 동작이 안 바뀜 |
| 4 | 「규칙 6개와 회귀 시료가 묶여 있음」 | **사실 아님** — 문맥 시료 13건 전부 `technical-report` · 발표 대본용 0건 · 기록에는 `register`·`axes` 축이 이미 있음 |
| 3 | 「발화 0회라 재배치 효과 없음」 | **철회** — 집단이 틀렸고, 제대로 재도 판정 불가 · 진짜 문제는 문맥 규칙을 글자로 찾는 것 |
| 5 | 「130줄 안팎」 | 재확인 · 133행 |
| 6 | 「우리」 유지 | 재확인 · 산문에서도 1.9배로 나오고 표본이 정당했음 |

## 이 표에 없는 것

- **오늘 이미 결정된 것** — 「불필요한 영어 줄이기」 예외 둘 · 전역 검사기 형식 축 · 오탐 3건 기록
- **확인 없이 한 기술 수정** — 계기판 짧은 라벨 대응 · 인자 처리 · 머리말 따옴표 · 스킬 연결. 언어 정책을 만들지 않는 변경이라 스킬 규정상 확인 대상이 아님
