---
form: structured
---

# 현재 상태 — 2026-09-08

**한글 검사 시스템이 지금 무엇을 하고 무엇을 못 하나** — 한 장 조망 · 판단 대기는 「판단이 필요한 것」 · 실행 기록은 `runs/`

## 만들고 있는 것

**검사 대상은 한국어 표현** — 문장이 바로 쓰였는지를 본다.

**★ 보는 시점은 문서로 나갈 때**(2026-08-31 확정) 보고·공유·제출용으로 글이 파일에 적히는 순간이 대상이다. 터미널 응답과 대화는 대상이 아니다.

- **못 박아 두는 이유** — 응답 쪽에서 문체 문제가 실측된 적이 있어(빈 명사 7.9배) 검사기를 그리로 넓히자는 생각이 되풀이됐다. 넓히면 이 시스템이 무엇인지가 흐려진다.
- **문서에서는 그 문제가 안 남** — 같은 구문이 사용자 문서 1만자당 0.90 · 내 문서 0.52로 오히려 적다.
- **★ 낱말별 재측정 — 여섯 중 셋이 갈림**(2026-09-02 · 앞의 「11건 전부 정상」을 뒤집음) 그 11건은 여섯 낱말을 **한 덩어리로** 센 수치였다. 실문서 1,161개로 따로 재니 `값` 9건·`축` 4건·`층` 2건은 그 명사가 혼자 서서 전부 정당했고, `판`·`창`·`결`은 **0건**이었다. 앞의 셋은 사람이 읽고, 뒤의 셋은 전역 검사기가 주의로 낸다.
- **뭉뚱그린 실측의 대가 — 규칙 하나를 통째로 접음** 갈라 재기 전까지 「결정 규칙으로는 못 넣는다」로 닫혀 있었다. 평균이 섞이는 항목은 낱말·갈래별로 쪼개 재야 한다.
- **응답 쪽 규율은 글로벌 CLAUDE.md 소관** — 이 저장소가 맡는 일이 아니다.

**검사기 둘의 규칙은 겹치지 않음** — 실측 380건에서 같은 줄을 둘 다 지적한 것이 5줄 · 무게도 이미 갈려 있음

| 무게 | 무엇 | 건수 |
|---|---|---|
| **오류** · 발행을 막음 | 서술형 종결 1,352 · 제목·진입점 101 · 모호한 지칭 64 · 절단형 종결 48 | 1,565 |
| **주의** · 참고 | 스캔 가치 없는 라벨 189 · 절단형 의심 145 · 해설을 인용 부호로 씀 85 · 형식 미표기 49 등 | 536 |

*문서 394건 기준 · 2026-09-01*

## ★ 지적의 70%가 켜고 끄는 갈래 (2026-09-08 실측)

**개조식 전제 아홉 갈래** — 서술형 종결 · 서술형 문단 · 제목 셋 · 진입점 넷 · 머리말 `form: prose` 한 줄로 통째로 꺼짐. 그 아홉을 빼고 세면 숫자가 이렇게 갈린다.

| 뭉치 | 오류 문서 | 개조식 규약 뺀 뒤 | 지적 중 규약 비중 |
|---|---:|---:|---:|
| 업무 보고서 620개 | 33% | **8%** | **70%** |
| 규칙·기록 962개 | 20% | **2%** | **71%** |

- **33% 를 홑겹으로 내세우면 안 됨** — 과장이기 이전에 **읽는 사람이 자기 숫자를 예측하지 못한다.** 개조식을 안 쓰는 곳에서 받아 돌리면 8% 가 나온다.
- **서술형 종결은 한국어 오류가 아님** — 「대체하지 않는다」는 문장으로는 멀쩡하고 개조식 문서의 값 칸에서만 어긋난다. 이중 피동·번역투와 같은 칸에 두면 안 된다.
- **가르는 기준은 검사기가 이미 들고 있음** — `FORM_ONLY_STRUCTURED` · 갈래가 늘면 따라 늚
- **딸려 나온 결함 하나** — 그 아홉을 세다가 「진입점 형태 확인」이 목록에서 빠져 산문 문서에 지적을 내던 것이 나왔다(고침 `7b5acb5` · 시험 둘). 2026-08-28 「서술형 문단」과 같은 모양이라 낱말을 더 적는 대신 시험으로 옮겼다.
- 실측 → [`runs/eval-010`](../runs/eval-010/) · 사례집 §01 이 쓰는 값

**시각 서식은 아예 안 봄**(2026-08-28) — 뺀 검사는 굵게 개수·묶음 행 수·강조 밀도 셋. 검사기의 목적은 「한글이 똑바로 써졌나」고 서식은 그 축이 아니다. 그 셋이 주의 2,546건 중 2,239건(88%)을 먹어 낱말 지적을 덮고 있었다.

**사람이 읽을 한글 문서를 전달 전에 검사하는 두 층 시스템** — 결정 규칙 스크립트(1층)가 명백한 것을 잡고, 읽는 판단(2층)이 나머지를 본다. 2층의 오탐은 1층 규칙으로 되먹인다.

**부품과 담당은 따로 봄** — [시스템 구조와 역할](architecture.md)

| 구성 | 위치 |
|---|---|
| 탐지 논리 정본 | `~/.claude/skills/finalize-korean-document` |
| 문서 구조 검사 | `~/.claude/assets/doc-style-check.py` |
| 측정·시험·기록 | 이 저장소 |

## ★ 규칙을 넣을지 정하는 관문 (2026-09-01 사용자)

**✅ 승격 완료 (2026-09-03)** — 정본은 글로벌 `~/.claude/references/work-principles.md` 「검사·규칙을 설계할 때」의 첫 하위 절. 아래는 이 저장소의 실측과 적용 결과.

**이 저장소가 이 관문으로 넣은 것 둘** — 이중 피동(뭉치 0건 · 문법서가 오류로 다룸) · 지어낸 명사구 판·창·결(실문서 1,161개 0건 · 정규식 한 줄)

**실측은 제 몫을 함** — 넣고 실문서 409건에 돌리니 오탐 둘이 나왔다.

| 갈래 | 잘못 걸린 것 | 처리 |
|---|---|---|
| 이중 피동 | 「구축되지는 않습니다」 · 「-지 않다」 부정형 | 어간에서 「되」를 뺌 |
| 이중 조사 | 「경로의」·「대로의」 · 「로」가 낱말의 일부 | `LO_WORD` 목록으로 막음 |
| 「~에 있어」 | 8건 **전부** 「저장소에 있어 접근이 안 된다」 | **결정 규칙에서 뺌** · 판단 층 소관 |

**마지막 것은 폐기가 아님** — 줄만 봐서 안 갈리는 갈래라 읽는 층 소관 · 「불필요한 영어」와 같은 처리

## ★ 두 층을 나누는 원칙 (2026-09-01 사용자)

**✅ 승격 완료 (2026-09-03)** — 정본은 글로벌 `~/.claude/references/work-principles.md` 「검사에서 애매한 항목을 처리하는 세 갈래」 · 글로벌 `CLAUDE.md` 「무엇을 검증할지」에 요약 한 줄. 아래는 이 저장소의 실측과 적용 결과.

**세 갈래 중 이 저장소가 쓴 둘** — 등급 내리기와 층 옮기기. 침묵은 이 검사기에 해당 없음(지적을 사람이 읽는다).

| 처리 | 이 저장소의 사례 | 결과 |
|---|---|---|
| 등급 내리기 | 문단 줄바꿈을 절단형 오류로 보던 것 | 49건 오류 → 주의 · 갈래 합계 193건 그대로 · 빠진 것 0건 |
| 층 옮기기 | 「~에 있어」 · 실문서 8건 전부 위치 표현 | 결정 규칙에서 빼고 판단 층으로 옮김 |

**규칙으로 좁히고 읽는 층이 거르는 것이 싼 이유** — 문서 394건 2,812,207자에서 1층이 낸 지적 문구는 119,638자로 **4.3%**다. 같은 것을 읽기로만 찾으려면 **24배**를 읽어야 한다.

**⚠️ 이 셈이 성립하는 조건 — 규칙이 그 갈래를 잡을 수 있을 때뿐**

| 갈래 | 처방 |
|---|---|
| 표면 모양이 고정됨 · 서술형 종결 1,352 · 절단형 97 | 넓게 잡고 거르기 · 오탐 감수 |
| 낱말이 그 자리에 맞는지 · 불필요한 영어 · 번역투 | 전수 읽기 말고 길 없음 · 좁히면 0건 넓히면 정상 문장 |

**뒤쪽에서는 거를 후보가 아예 안 생김** — 참 문제 13건 중 1층이 0건을 잡았다. 사용자도 「이슈」·「컨펌」·「타깃」을 쓰므로 넓힐 정규식 자체가 없다.

**★ 결정 규칙으로 닫힌 경로 셋** — 셋 다 만들어 재 보고 닫았다. 폐기가 아니라 판단 층으로 옮긴 것이다.

| 갈래 | 닫힌 이유 | 기록 |
|---|---|---|
| 불필요한 영어 | 사용자도 그 낱말을 씀 · 문맥이 가름 | `runs/eval-005` |
| 「~에 있어」 | 「저장소에 있어 접근이 안 된다」와 안 갈림 · 실문서 8건 전부 위치 표현 | 2026-09-01 |
| 명사로 굳힌 서술 | **개조식 라벨이 곧 명사 나열** · 정밀도 15%(느슨) · 55%(좁힘·회수 1/3) | `runs/diagnostic-009` |

**1층 천장이 구조적임** — 참 문제 35건 중 이 셋이 17건(49%)이다. 규칙을 더 만들어도 절반은 안 잡힌다.

**승격 후보: DESIGN** — 검사기·탐지기 설계 일반에 걸리는 원칙이라 이 저장소에만 둘 것이 아니다. 승격 여부는 사용자 판단.

## 지금 성적

**★ 잡을 수 있는 것과 실제로 잡는 것을 갈라 읽을 것** — 아래 회수율은 「그 층이 돌았을 때」의 값이다.

| 측정 | 값 | 믿을 수 있나 |
|---|---|---|
| 1층 회수율 | 0.057 | 문서 3건 전수 판정 · 분모가 참 (`runs/eval-005`) |
| 2층 회수율 | 0.771 | 같은 측정 |
| 2층 정밀도 | 하한 0.800 · 상한 0.850 | 사용자 판정이 라벨이라 순환 없음 |
| 1층 오탐 | 음성 34건에서 0건 | 그대로 |
| 처방이 만드는 새 지적 | 51쌍에서 0건 | 검사기가 보는 범위 한정 |
| 옛 회수율 0.846 | **못 씀** | 라벨을 2층과 같은 통과가 만들어 순환 |

**갈래가 성적을 가름** — 1층이 잡는 것은 표면 모양이 고정된 위반(서술형 종결·조사로 끊김·「우리」)이고, 문서 391개에서 오류 1,558건을 낸다. 못 잡는 것은 낱말이 그 자리에 맞는지다(불필요한 영어 13건 중 0건). `eval-005`의 세 문서가 마침 뒤쪽이 많은 조사 보고서였다.

## 운영 중 실제로 도는가

| 부분 | 도는가 | 근거 |
|---|---|---|
| 전역 문서 검사기 + 발행 훅 | **도는 중** | 8/29~31 세션 7개 · 지적 418건 · 시각 서식 제거도 먹힘 |
| 2층 읽기 | **거의 안 돎** | 한글 문서를 쓴 세션 55개 중 2개(3.6%)만 호출 |
| 피드백 기록 | 사람이 태워야 돎 | 통로는 있고 자동은 아님 |
| 후보 승격 | **안 함 · 확정**(9/1) | 11건 중 글자 대조가 되는 형태가 1건뿐 · 2층은 이미 읽는 중 |

**그래서 운영 중 실제로 걸러지는 것은 1층이 잡는 것뿐** — 참 문제의 6% 남짓
**고친 것**(8/31) — 2층을 부르라는 안내를 발행 경로로 옮겨 1층 발화와 무관하게 냄 · 효과는 `runs/eval-006`에서 며칠 뒤 판정

## 이번에 확인한 것

- **산문 문서에서 개조식 검사가 쏟아짐** — 사용자가 쓴 안내 메일 오류 9건 전부 오탐 · 형식 축으로 13.51 → 1.10건 (`runs/diagnostic-004`)
- **처방이 만드는 새 지적** — 51쌍에서 0건 · 심어 넣은 연쇄 3건은 잡음 (`runs/diagnostic-007`)
- **뜻이 변질되는 것** — 낱말 대조로는 못 잡음 · 표지 여섯 갈래로 심어 넣은 변형 6건 전부 잡고 대조군 5건 침묵
- **통문장 규칙 6건은 어떤 형태로도 못 돎** — 좁히면 다시 안 나오고 넓히면 정상 문장을 잡음 (`runs/diagnostic-006`)
- **규칙 후보 12건 전부 미채택** — 가장 많이 걸린 것이 문서 27%에서 정밀도 3% (`runs/eval-002`)

## 지금 실제로 도는 것

| 층 | 검사 대상 | 실행 위치 |
|---|---|---|
| 스킬 검사기 | 확정 표현 8건 · `경로` 비유 | 훅·직접 실행 |
| 전역 문서 검사기 | 개조식 구조 · 절단형 · 모호한 지칭 · 평가 수식어 · 번역투 셋 · **지어낸 명사구(판·창·결)** | 훅(쓰는 순간·발행 직전) |
| 형식 축 | 산문 문서에서 개조식 검사를 끔 | 머리말 `form: prose` 또는 `--form prose` · 훅도 같은 정의를 씀 |
| 판단 층 | 나머지 일곱 원칙 · 지어낸 명사구의 값·축·층 | 사람·모델이 읽고 판단 |
| 의미 표지 | 낱말이 남아도 뜻이 빠진 곳 | `check_meaning.py` 보고 |
| 되먹임 | 오탐을 규칙 예외로 기록 | `record_feedback.py --user-verdict false_positive` |
| 되먹임 확인 | 적은 예외가 판단 층 행동을 바꾸나 | `scripts/probe_layer2_exemption.py` · 규칙 바뀔 때 1회 |
| 연쇄 확인 | 처방이 새 지적을 만드는지 | `scripts/check_fix_cascade.py` |
| **덮개 계기판** | 쓴 문서 중 검사 범위에 든 비율 · 실측 `runs/eval-008` | `scripts/measure_gate_coverage.py` |
| **일일 건강 검진** | 하루 첫 세션에 회귀 · 초록이면 침묵 | `~/.claude/hooks/korean-gate-daily-check.py` |

## 안전장치

- **시험 300여 건** · 저장소 시험이 스킬 자체 시험까지 실행
  (클론한 기계 315건 · 작성자 기계 342건 — 사내 문서를 대조하는 시험이 클론에서는 건너뛴다)
- **배포본을 지킴** — 스킬을 망가뜨리면 저장소 시험이 실패 (사본을 두지 않음)
- **돌연변이로 확인** — 넣은 안전장치를 하나씩 부숴 시험이 잡는지 본다 · 8/31 이후 86건 전부 잡힘
- **계기판** — 규칙을 더했을 때 실제로 더 잡는지 숫자로 확인 (`scripts/eval_detectors.py`)

## 아직 못 하는 것

- **회수율이 참인 범위가 세 문서뿐** — 분모는 전수 판정으로 참이 됐지만(`runs/eval-005`) 참 문제 35건이라 신뢰 구간이 넓다. 다른 종류의 문서로 넓히려면 또 전수 판정이 필요하다.
- **검사기가 보는 원칙은 11개 중 4개** — 나머지 일곱은 사람이 읽어야 하고, 그 판단의 회수율은 못 잼
- **판단 층 준수는 한 건만 확인됨**(2026-09-02) — 「에이전틱」 예외를 실제로 지키고 확정 교정을 글자까지 재현했다(`runs/eval-007`). 나머지 원칙과 다른 문서로는 안 넓혔다 · 넓히려면 호출당 0.3달러
- **연쇄 확인은 문장 단위 한정** — 문단·문서 단위 연쇄는 못 봄

## 다음

**판단 대기 하나**(2026-09-03 아키텍처 검토) — 넷은 그날 닫음 · 남은 것은 전역 자산이 없을 때 시험이 조용히 건너뛰는 것 → [판단이 필요한 것](decisions-pending.md)
**혼자 도는 것 하나** — 2층 안내 효과 재측정 · 며칠 뒤 `python -X utf8 scripts/measure_layer2_uptake.py --days 14` (`runs/eval-006`)
**그 결과가 가르는 것** — 발화율이 올랐는데 이행율이 안 오르면 문구가 아니라 다른 장치가 필요함 · 그때가 다음 설계 지점

## 부서원 공유 — 저장소 공개 준비 (2026-09-07)

**GitHub 비공개 저장소로 올림** — <https://github.com/kkhfiles/korean-writing-qa> · 공개 전환이 목적

| 무엇 | 상태 |
|---|---|
| 정본 위치 | **이 저장소** · `~/.claude/` 는 `install.py` 가 만드는 설치본 |
| 라이선스 | **MIT**(`LICENSE`) · 심긴 서체만 SIL OFL 1.1(`NOTICE`) |
| 받는 법 | README 첫 절이 클론부터 시험까지 명령 순서 · 단계마다 나와야 하는 값 |
| 갈래 고르기 | `korean-qa.toml` 로 27갈래를 끄고 낮춤 · 끈 것은 출력에 찍힘 · 기본으로 꺼진 「반말 서술형」은 `on` 으로 켬 |
| 사례 받기 | 이슈 서식 둘 · 받은 문장이 회귀 시료가 됨 · `CONTRIBUTING.md` |
| CI | 3.11·3.13 두 판에서 시험·스킬 자체 검사·사례집 오류 0 |
| 사내 내용 | 작업 트리와 **이력 96개 커밋에서 모두 걷어냄** · 블롭 773개 전수 확인 |

**공개 전에 남은 것** — 회사 승인 · `LICENSE` 의 법인명 확인

**받는 사람이 겪는 상태를 실측함** — 임시 디렉터리에 클론해 돌리면 시험 통과 · 건너뛰기 14건은 작성자 기계에만 있는 연구 자료를 보는 시험이다.


**기술적으로는 막는 것 없음** — 바깥 라이브러리 0 · 개인 절대 경로 0(스킬 기본값 1곳은 환경 변수로 덮음) · 297KB · 시료 최대 94자라 문서 내용 노출 없음

**다듬을 곳으로 확인된 둘**

| 무엇 | 실측 | 진행 |
|---|---|---|
| 산문 문서에 개조식 검사가 쏟아짐 | 안내 메일 오류 9건 전부 오탐 · `form: prose` 를 모르면 안 붙임 | **일부 해소**(9/1) · 개조식 지적이 쏠리면 검사기가 형식을 물어봄 · 물음 8 → 49건 |
| 사내 낱말이 규칙 예시에 섞임 | 「CT」 35곳 · 밖에 주려면 일반 낱말로 바꿔야 함 | 그대로 |

**줄 때 나눌 수 있는 세 단계** — 검사기 하나(파이썬만 · Claude Code 불필요) · 스킬 전체 · 훅까지. 첫 단계가 지금 지적의 대부분을 냄

**설명 자료 준비됨**(2026-09-04 · 09-09 개정) — [사례집](showcase.html) · 절 여덟이 가치 순(보여 주기 → 근심 → 맞춤 → 규모 → 목록 → 근거 → 받는 법) · 수정 전후 74쌍 · 갈래별 비중 · 조직 관행에 맞추는 법. 실측 근거는 `runs/eval-010`
