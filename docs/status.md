---
form: structured
---

# 현재 상태 — 2026-08-27

**한글 검사 시스템이 지금 무엇을 하고 무엇을 못 하나** — 한 장 조망 · 판단 대기는 「판단이 필요한 것」 · 실행 기록은 `runs/`

## 만들고 있는 것

**검사 대상은 한국어 표현** — 문장이 바로 쓰였는지를 본다.

**★ 보는 시점은 문서로 나갈 때**(2026-08-31 확정) 보고·공유·제출용으로 글이 파일에 적히는 순간이 대상이다. 터미널 응답과 대화는 대상이 아니다.

- **못 박아 두는 이유** — 응답 쪽에서 문체 문제가 실측된 적이 있어(빈 명사 7.9배) 검사기를 그리로 넓히자는 생각이 되풀이됐다. 넓히면 이 시스템이 무엇인지가 흐려진다.
- **문서에서는 그 문제가 안 남** — 같은 구문이 사용자 문서 1만자당 0.90 · 내 문서 0.52로 오히려 적다. 검사기에 넣어도 정상 문장만 잡는다(세 뭉치 11건 전부 정상).
- **응답 쪽 규율은 글로벌 CLAUDE.md 소관** — 이 저장소가 맡는 일이 아니다.

**검사기 둘의 규칙은 겹치지 않음** — 실측 380건에서 같은 줄을 둘 다 지적한 것이 5줄 · 무게도 이미 갈려 있음

| 무게 | 무엇 | 건수 |
|---|---|---|
| **오류** · 발행을 막음 | 서술형 종결 1,273 · 절단형 종결 91 · 모호한 지칭 67 · 제목·진입점 96 | 1,527 |
| **주의** · 참고 | 스캔 가치 없는 라벨 180 · 절단형 의심 94 · 해설을 인용으로 84 등 | 487 |

**시각 서식은 아예 안 봄**(2026-08-28) — 뺀 검사는 굵게 개수·묶음 행 수·강조 밀도 셋. 검사기의 목적은 「한글이 똑바로 써졌나」고 서식은 그 축이 아니다. 그 셋이 주의 2,546건 중 2,239건(88%)을 먹어 낱말 지적을 덮고 있었다.

**사람이 읽을 한글 문서를 전달 전에 검사하는 두 층 시스템** — 결정 규칙 스크립트(1층)가 명백한 것을 잡고, 읽는 판단(2층)이 나머지를 본다. 2층의 오탐은 1층 규칙으로 되먹인다.

| 구성 | 위치 |
|---|---|
| 탐지 논리 정본 | `~/.claude/skills/finalize-korean-document` |
| 문서 구조 검사 | `~/.claude/assets/doc-style-check.py` |
| 측정·시험·기록 | 이 저장소 |

## 지금 성적

**★ 잡을 수 있는 것과 실제로 잡는 것을 갈라 읽을 것** — 아래 회수율은 「그 층이 돌았을 때」의 값이다.

| 측정 | 값 | 믿을 수 있나 |
|---|---|---|
| 1층 회수율 | 0.057 | 문서 3건 전수 판정 · 분모가 참 (`runs/eval-005`) |
| 2층 회수율 | 0.771 | 같은 측정 |
| 2층 정밀도 | 하한 0.800 · 상한 0.850 | 사용자 판정이 라벨이라 순환 없음 |
| 1층 오탐 | 음성 34건에서 0건 | 그대로 |
| 처방이 만드는 새 지적 | 51쌍에서 0건 | 검사기가 보는 범위 한정 |
| 옛 회수율 0.846 | **못 씀** | 라벨을 2층과 같은 통과가 만들어 순환 |

**갈래가 성적을 가름** — 1층이 잡는 것은 표면 모양이 고정된 위반(서술형 종결·조사로 끊김·「우리」)이고, 문서 391개에서 오류 1,558건을 낸다. 못 잡는 것은 낱말이 그 자리에 맞는지다(불필요한 영어 13건 중 0건). `eval-005`의 세 문서가 마침 뒤쪽이 많은 조사 보고서였다.

## 운영 중 실제로 도는가

| 부분 | 도는가 | 근거 |
|---|---|---|
| 전역 문서 검사기 + 발행 훅 | **도는 중** | 8/29~31 세션 7개 · 지적 418건 · 시각 서식 제거도 먹힘 |
| 2층 읽기 | **거의 안 돎** | 한글 문서를 쓴 세션 55개 중 2개(3.6%)만 호출 |
| 피드백 기록 | 사람이 태워야 돎 | 통로는 있고 자동은 아님 |
| 후보 승격 | 안 함 | 후보 11건 대기 |

**그래서 운영 중 실제로 걸러지는 것은 1층이 잡는 것뿐** — 참 문제의 6% 남짓
**고친 것**(8/31) — 2층을 부르라는 안내를 발행 경로로 옮겨 1층 발화와 무관하게 냄 · 효과는 `runs/eval-006`에서 며칠 뒤 판정

## 이번에 확인한 것

- **산문 문서에서 개조식 검사가 쏟아짐** — 사용자가 쓴 안내 메일 오류 9건 전부 오탐 · 형식 축으로 13.51 → 1.10건 (`runs/diagnostic-004`)
- **처방이 만드는 새 지적** — 51쌍에서 0건 · 심어 넣은 연쇄 3건은 잡음 (`runs/diagnostic-007`)
- **뜻이 변질되는 것** — 낱말 대조로는 못 잡음 · 표지 여섯 갈래로 심어 넣은 변형 6건 전부 잡고 대조군 5건 침묵
- **통문장 규칙 6건은 어떤 형태로도 못 돎** — 좁히면 다시 안 나오고 넓히면 정상 문장을 잡음 (`runs/diagnostic-006`)
- **규칙 후보 12건 전부 미채택** — 가장 많이 걸린 것이 문서 27%에서 정밀도 3% (`runs/eval-002`)

## 지금 실제로 도는 것

| 층 | 검사 대상 | 실행 위치 |
|---|---|---|
| 스킬 검사기 | 확정 표현 8건 · `경로` 비유 | 훅·직접 실행 |
| 전역 문서 검사기 | 개조식 구조 · 절단형 · 모호한 지칭 · 평가 수식어 | 훅(쓰는 순간·발행 직전) |
| 형식 축 | 산문 문서에서 개조식 검사를 끔 | 머리말 `form: prose` 또는 `--form prose` · 훅도 같은 정의를 씀 |
| 판단 층 | 나머지 일곱 원칙 | 사람·모델이 읽고 판단 |
| 의미 표지 | 낱말이 남아도 뜻이 빠진 곳 | `check_meaning.py` 보고 |
| 되먹임 | 오탐을 규칙 예외로 기록 | `record_feedback.py --user-verdict false_positive` |
| 연쇄 확인 | 처방이 새 지적을 만드는지 | `scripts/check_fix_cascade.py` |

## 안전장치

- **시험 121건** · 스킬 자체 시험 105 언어 단언 · 저장소 시험이 스킬 자체 시험까지 실행
- **배포본을 지킴** — 스킬을 망가뜨리면 저장소 시험이 실패 (사본을 두지 않음)
- **돌연변이로 확인** — 이번에 넣은 안전장치 12개 전부 무력화 시험을 통과
- **계기판** — 규칙을 더했을 때 실제로 더 잡는지 숫자로 확인 (`scripts/eval_detectors.py`)

## 아직 못 하는 것

- **회수율의 분모가 참이 아님** — 라벨이 「누가 알아챈 것」의 기록 · 판정 시트를 문서별로 나눠 뒀고(`runs/eval-004`) 26·23·23행 판정이 남음
- **검사기가 보는 원칙은 10개 중 3개** — 나머지는 사람이 읽어야 하고, 그 판단의 회수율은 못 잼
- **연쇄 확인은 문장 단위 한정** — 문단·문서 단위 연쇄는 못 봄

## 다음

**판단 대기 한 건** → [판단이 필요한 것](decisions-pending.md) · 1번(훅 통일)만 실사용에 영향이 크고 나머지는 안 해도 시스템이 돈다.
