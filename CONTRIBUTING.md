---
form: structured
---

# 사례를 보내는 법

**어색한 한국어를 하나 발견했을 때 할 일** — 이슈로 문장 하나 · 회귀 시료로 박혀 규칙이 바뀔 때마다 다시 돌아감 · 코드 지식 불필요

## 두 갈래

| 무엇 | 이슈 서식 | 예 |
|---|---|---|
| 고쳐야 하는데 안 잡힘 | 「안 잡히는 표현」 | 「고칠 것은 코멘트로」가 그냥 지나감 |
| 멀쩡한데 잡힘 | 「멀쩡한데 잡힌 표현」 | 「국회 회기」를 오타로 지적 |

**오탐이 미탐보다 비쌈** — 멀쩡한 문장이 몇 번 걸리면 사람이 검사 결과를 통째로 안 보기 시작하고, 그때부터 진짜 지적도 함께 묻힌다.

## 이슈에 꼭 적을 것

- **문장 하나** — 문제가 되는 줄만 · 문서 전체는 불필요
- **왜 그것이 맞는지** — 「어색해서」만으로는 남이 판단할 수 없다. 무엇이 사라졌는지, 무엇으로 읽히는지를 적는다.
- **★ 비슷하게 생겼지만 잡으면 안 되는 것** — 가장 값진 정보다. 규칙을 넓혔을 때 멀쩡한 문장이 걸리는지를 이것으로 잰다. 없으면 규칙을 넓히다 오탐을 못 보고 지나간다.

## 설정으로 먼저 풀리는지 확인

**조직마다 문서 관행이 다름** — 표 머리 「비고」는 공문서 표의 표준 관례이고, 발표 대본은 산문이 정상이다. 그 조직에서만 안 잡히면 되는 것이면 규칙을 바꾸지 않아도 된다.

```
python assets/doc-style-check.py --list-rules
```

**끄는 법** — `korean-qa.toml` 에 그 갈래 이름 · 보기는 `korean-qa.example.toml`

## 직접 고쳐서 PR 을 보낼 때

**순서 고정 — 사례가 먼저, 규칙이 나중** — 거꾸로 하면 무엇을 고쳤는지 증명할 길이 없다.

### 1. 사례를 시료로 등록

```
python scripts/add_case.py --text "고칠 것은 코멘트로" --expect finding \
    --why "조사로 끝나 미완성으로 읽힘" --source "#12"
```

**`--expect` 값 둘** — `finding`(잡혀야 함) · `clean`(통과해야 함). 스크립트가 지금 판정을 재서 상태를 정한다.

| 상태 | 뜻 | 시험 처리 |
|---|---|---|
| `open` | 아직 규칙 없음 | 실패가 예정된 것으로 처리 |
| `pinned` | 규칙이 들어가 실제로 맞음 | 깨지면 실패 |

### 2. 규칙 수정

| 어떤 갈래 | 고칠 곳 |
|---|---|
| 글자만 보고 갈리는 것 | `assets/doc-style-check.py` |
| 문맥을 읽어야 갈리는 것 | `skills/finalize-korean-document/references/core-rules.md` |

**가르는 기준은 겉모습** — 문자열 대조로 갈리면 검사기 · 앞뒤를 읽어야 갈리면 규칙 문서

### 3. 상태 올리기

```
python scripts/add_case.py --recheck
```

**안 올리면 시험이 알림** — `open` 이던 사례가 통과하기 시작하면 「예정에 없던 통과」로 뜬다. 그대로 두면 나중에 그 규칙이 되돌아가도 아무도 모른다.

### 4. 실문서로 오탐 측정

**여기가 가장 중요함** — 규칙을 넓히면 무엇이 함께 걸리는지 봐야 한다. 한글 문서가 여럿 있는 디렉터리에 규칙 적용 전후로 검사기를 돌리고, 새로 생긴 지적을 하나씩 본다.

```
python assets/doc-style-check.py <문서 디렉터리> -v --no-rules > 전.txt
# 규칙을 고친 뒤
python assets/doc-style-check.py <문서 디렉터리> -v --no-rules > 후.txt
diff 전.txt 후.txt
```

- **파일 목록을 그대로 두고 잴 것** — 그 사이에 문서를 고치면 행 번호가 밀려 diff 가 온통 그것으로 찬다.
- **PR 본문에 적을 것** — 새로 생긴 지적 수 · 그중 정당한 것의 수. 정당하지 않은 것이 하나라도 있으면 규칙이 너무 넓다.

### 5. 시험

```
python -X utf8 -m unittest discover -s tests
```

**건너뛰는 것이 있어도 정상** — 설치된 Claude Code 환경을 보는 시험은 그 환경이 없으면 건너뛴다.

## 받아들이는 기준

- **넣는 쪽** — 정당한 쓰임에서 오탐 0 · 실문서에서 실제 발화가 있는 갈래
- **안 넣는 쪽** — 문맥을 읽어야 갈리는 것. 검사기에 넣으면 오탐이 섞이고, 오탐이 섞인 검사는 아무도 안 본다. 그런 것은 규칙 문서(`core-rules.md`) 소관.
- **판정이 갈리는 쪽** — 오류가 아니라 **주의**로 냄 · 사람이 보고 고름

## 문서를 고칠 때

```
python assets/doc-style-check.py <고친 파일> --no-rules
```

- **발행물** — `docs/showcase.html` 은 오류 0 이 아니면 CI 가 막음
- **작업 문서** — 강제하지 않음
- **산문이 뼈대인 문서** — 머리말에 `form: prose` 한 줄 · 개조식 전제 검사가 꺼짐

## 보내 주시는 것의 조건

**MIT** — 이슈로 보낸 사례와 PR로 보낸 코드는 저장소와 같은 조건(`LICENSE`)으로 들어간다. 따로 서명할 서류는 없다.

- **남의 글을 옮겨 오지 않음** — 사례는 직접 겪은 문장이거나 스스로 지어낸 것. 다른 문서에서 그대로 가져온 문장은 그 문서의 조건이 따라온다.
- **기밀이 든 문장을 그대로 보내지 않음** — 고쳐야 할 표현만 남기고 내용은 바꿔서 보낸다. 회귀 시료는 공개 저장소에 그대로 박힌다.
