__pycache__/
*.py[cod]
.pytest_cache/
.venv/

# 원문 복사본은 로컬 실험 데이터다.
data/raw/**
!data/raw/README.md

# 실행 중 만들어지는 임시 출력은 로컬에만 둔다.
runs/**/working/
runs/**/raw/

*.tmp
*.bak
.hook-sandbox/

# 이름 목록은 공개 저장소에 두지 않는다 — scrub_freq_table.py 가 로컬에서만 읽는다.
data/catalog/local-identity-denylist.txt
# 제품·상표 이름 정본 목록 — 본보기(`*.example.txt`)만 올라간다
data/catalog/local-brand-names.txt
data/catalog/local-config-repo.txt
# 남의 프로젝트 경로로 범위를 좁힌 정상 판정 — 공개하면 그 경로가 나간다(rare_words.py)
data/catalog/local-known-words.jsonl
